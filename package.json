{
  "name": "miguel",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -Rf tmp/* && babel src -d tmp && yarn uglifyjs lib/phaser.js tmp/states/base.js tmp/*.js tmp/**/*.js -o miguel.js -b"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "uglifyjs": "^2.4.11"
  },
  "dependencies": {
    "uglify-js": "^3.4.9"
  }
}
